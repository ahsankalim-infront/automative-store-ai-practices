-- AutoZone Store — MySQL Schema
-- Run files in order. Switch DATA_SOURCE=mysql in .env after setup.

CREATE DATABASE IF NOT EXISTS autozone_store
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;

USE autozone_store;
